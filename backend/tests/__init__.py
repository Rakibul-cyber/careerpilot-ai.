# tests package.
# Automated test suite (pytest). Mirror the app/ structure here, e.g.
#   tests/services/, tests/api/, tests/repositories/.
# Add conftest.py for shared fixtures (test DB, app client) as tests are written.
