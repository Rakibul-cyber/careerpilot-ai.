# utils package.
# Small, stateless, reusable helpers with no dependency on app layers
# (e.g. datetime helpers, pagination, string/slug utilities).
# Nothing with business meaning belongs here — that goes in services.
