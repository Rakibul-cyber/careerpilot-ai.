# app package root.
# Marks `app` as the top-level Python package for the CareerPilot AI backend.
# All application layers (api, core, db, models, schemas, services, repositories)
# live beneath this package.
