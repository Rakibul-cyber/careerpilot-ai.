# api.v1.endpoints package.
# One module per resource/feature (e.g. auth.py, users.py, resumes.py, jobs.py).
# Each endpoint module defines route handlers that validate input via schemas
# and delegate all work to the services layer — no business logic lives here.
